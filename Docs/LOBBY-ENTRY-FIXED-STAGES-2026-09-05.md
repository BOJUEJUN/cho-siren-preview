# 大厅入口合并与取消适配试演

## 用户当前明确选择

2026-09-05 19:50之后，用户要求“开始演出”直接进入原“冒险剧本”页面，并移除单独的“冒险剧本”按钮。随后询问适配试演与持续自动攻击，最终明确要求“取消适配吧”。

本批仅处理上述范围：

- 大厅中央“开始演出”的外观、位置与副标题不变，点击打开原章节地图。
- 不再创建单独的冒险剧本装置、标签或隐藏点击区域；保留任务、闪耀舞台、HUD和底部导航。
- 彻底移除适配试演入口、等级档提示和按队员等级放大敌方属性的分支，不只是隐藏按钮。开战、战力预览和结算统一使用原关卡强度。
- 不改玩家等级、编队、资产、首通、星级、角色成长公式和原关卡费用/奖励。高等级打低级关可快速获胜，不再用动态加血维持演出时长。
- 普攻和双技能仍按各自冷却自动释放；“手动重投/自动重投”只控制骰子。保留单排按钮、“剩余”倒计时及移除错误总血条阶段刻度的修复。本批不改攻击节奏或特效。
- 旧独立节奏演出组件和素材保留作历史功能，并改为独立组件测试；不重新给它增加另一个大厅入口。

`Docs/HIGH-LEVEL-REHEARSAL-2026-09-05.md`及9e90542是此前历史记录，不再代表用户当前选择。以后不能因为旧测试或旧发布记录重新恢复适配试演。

## 验证与发布记录

仅入口改动的首次PlayMode为48/49：新增测试返回大厅后立即点击，早于入口动画恢复可交互；测试加入有上限的真实就绪等待后，49/49通过。随后用户追加取消适配，需以合并两项要求后的完整结果为最终验收。

合并后EditMode `TestResults/editmode-fixed-entry-20260905.xml` 272/272、PlayMode `TestResults/playmode-fixed-entry-20260905.xml` 48/48，进程0。包含1/68/100级同一关卡敌方属性完全一致、旧档等级与资产保持、战力预览同源、无适配UI节点、中央入口完整章节/剧情/战斗链路，以及0体力仍可浏览地图并重复返回。测试数量变化是删除4项被取消模式的逻辑测试并新增1项固定强度回归，以及调整运行时入口测试，不是跳过失败项。

源码检查点 `ece32db`，Unity构建 `Builds/WebGL-Fixed-Entry-20260905` 成功、进程0（日志 `Logs/build-fixed-entry-20260905.log`，构建约1分33秒）。临时HeroFrames排除已自动恢复，构建后tracked工作树干净。

新包隔离Chrome390×844合成旧档实测：大厅独立冒险入口消失；点击中央开始演出打开1-10地图，战力4,735、按钮“再次挑战”，无适配/等级档标签。1-10实际敌方总生命恢复36,880，约3秒三星取胜，结算返回地图/大厅再入正常；这次快通关是固定强度与旧高等级队伍的预期结果，不作为需要恢复适配的理由。浏览地图体力120不变，开战后106，重复浏览仍106；等级与其他存档数据保留。双面试池、顶部和底部目视检查无回退，截图 `Artifacts/qa-20260905/fixed-entry-*.png`；无pageerror。

`Tools/Stage-WebGL.mjs` 5/5、暂存后 `npm run check` 通过（含12项加载器测试）。候选Pages `5a8d99d` 以9e90542为父版本，保留9e90542完整资源，轮换更早a7d14c7四个资源；后者可从Git历史恢复。没有删除用户素材或改动浏览器真实存档。正式站验收待下方追加。

本批资源：

- `a1ded1ec763365ae5eb96f437a067b69.data.unityweb`，74,313,372字节。
- `0b914ce157afdd57a087c0e569ac8a0a.framework.js.unityweb`，78,094字节。
- `6900fe895d9b58048a52bee3935b4102.wasm.unityweb`，9,001,526字节。
- `eb4e632fb655b67fcfd27e3443495fc7.loader.js`，41,128字节。

QA内部HTTP服务器首次被sandbox禁止监听，在允许的本机回环地址提升权限后完成测试并关闭；不向用户交付该内部地址。最终只交付既有GitHub Pages网址。

## 正式上线验收（20:14）

- Pages快进推送成功，HEAD与origin/main均为 `5a8d99d6079db87bdd995bd8d52d90a735797855`。GitHub仅提示data超过推荐50MiB，未达到100MiB限制，推送正常；没有推送Unity源码分支。
- 官网 `https://bojuejun.github.io/cho-siren-preview/?v=5a8d99d` 清单与本地一致；HTML SHA-256为 `4257c5b6d49cd64b2067663c64f76b18859f504922eb6d92b36081c5483ea12b`，与本地一致。四个新资源均HTTP200、Content-Length与本地一致。
- 官网隔离Chrome新档实测：大厅无独立冒险剧本按钮，点击中央“开始演出”直接进入1-1章节地图；点击“开始挑战”进入固定强度战斗，12秒三星胜利、0倒下。确认结算、体力120→112、解锁1-2，返回大厅再次进入地图仍为112体力，没有浏览重复扣费。
- 官网地图、战斗和结算均无适配试演/等级档提示；顶部单排“手动重投/1倍/暂停”和自动攻击保留。HUD、底部导航、线上及线下面试目视检查通过，无pageerror。截图 `Artifacts/qa-20260905/official-fixed-entry-*.png`。
- 旧高等级存档在新包的浏览器验收见上文；测试浏览器为临时独立上下文，用户实际网页存档未被访问或修改。未宣称真实iPhone/Windows已验收，也未扩展到战斗特效或整体数值重做。

本批两项明确请求完成。以后继续修改应以当前用户选择和最新合并分支为准，不从9e90542的适配版本重新发布覆盖。
