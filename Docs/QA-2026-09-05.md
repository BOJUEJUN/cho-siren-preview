# 持续优化检查记录 · 2026-09-05

本轮目标：完整检查正式 WebGL 主流程和手机/桌面布局，按问题证据修复并更新正式 GitHub Pages。目标保持进行中，不能用单项测试代替整体验收。

## 基准与所有权

- 起点：Unity 源码 `801fae5`；正式发布 `ce95986`。
- 正式链接：https://bojuejun.github.io/cho-siren-preview/
- 已检查远端和全部工作树，源分支包含已接受的 HUD、章节、选秀改动。构建前再次核对远端。
- 本任务修改 `GameModel.cs`、`Panels/GachaPanel.cs`、对应测试和本记录。其他任务已有 `.agents/`、`Assets/Live2D/`、`Artifacts/`、`Tools/render_character_preview.py` 保留。
- 顶部 HUD、底部导航、用户确认的角色/舞台素材作为视觉保留基准。

## 已确认的问题

1. 候选名单在每次查看时由“剩余未解锁角色”重算，签约会改变模数与偏移，导致其他候选跳动、自动补人；不是当日固定十名名单。已修改为稳定 ID 持久化快照，两池一并初始化，签约只隐藏已签约者，18:00 换期。
2. 签约使用 UI 传入价格，模型不核对当前池、期次或报价；跨刷新边界可能按旧名单签约。已添加正式面试入口校验，旧接口保留兼容。
3. 面试界面跨 18:00 不主动刷新，重建时旧卡等到帧末才销毁。已添加换期检查，旧内容即时停用，防止短暂重叠和重复点击。
4. 交接文档仍允许 SSR 和三卡池/十连，容易引导后续任务恢复旧界面。已更新选秀约束。
5. 候选“魅力收益加成”只是面板计算值，`Perform()` 固定发放 520 金币，未参与收益结算。待修复。
6. 舞台四维和技能描述在选秀面板按角色索引计算，缺少与真实战斗数据的统一来源。待核对并修复，不能把伪造的技能描述当作已接通。
7. 当前角色表只有主唱、舞者、支援三种职业，默认四人队中存在两名主唱；与用户“四个不同位置”要求不一致。需从企划确定完整四职业定义后落地，避免凭空改角色。
   - 已回查用户 PPT：职业总表为主唱、主舞、Rapper、DJ；人物页另有“高音”，应作为能力/技能描述处理。四种族为魅族、魔族、海灵族、血精灵。
8. 实际角色表 64 人（旧文档仍写 54），但战斗表只有最早 9 名成员。其余 55 人会被 `BuildParty()` 静默跳过。已基于既有同职业单位模板补全 55 名成员的真实属性和技能（按基础战力缩放，保留原九人和敌人），并增加完整编队检查，不能少人后仍然扣体力开始。
9. 成员详情使用伪造属性、技能和全队百分比加成，未读取战斗表。已改为实际等级成长属性与真实技能描述，移除无实际效果的编队加成承诺。
10. 成员详情美术是左右两个技能槽，文字却全部堆在左侧。已把两项技能分别放回对应槽内，待新构建截图复核。

## 最新 PDF 对齐批次

- 已收到并核对用户新 PDF，实施边界详见 `LATEST-DESIGN-2026-09-05.md`；不是整份新玩法已完成。
- 64 名成员职业与战斗表、筛选、选秀推荐已统一为四职业；编队模型校验四职各一名，旧成员/等级/存档保留，错误编队不扣体力。
- 团队页的虚构“共鸣评分/组合展示数”改为实际四职业就位/缺位提示，保留原布局及装饰。
- 八种骰型按 PDF 调至 1.0-2.0；战斗预览和真实行动均有倍率上限；UI 精确显示 1.15/1.25/1.45。
- 十关原 HP 与新倍率不匹配，先按 60% 调整独立 HP，1-7 再调为 875‰；攻击、防御与奖励不变。不是新怪物表的全面替换。
- 最新逻辑测试：`TestResults/editmode-pdf-alignment-v3.xml` 225/225；十关默认四人、1.4 倍测试各为 9/8/9/10/9/10/9/10/10/10 次玩家行动。真实秒级限时尚未实装，操作次数不能冒充时长实测。
- 成员技能面板已通过本地独立浏览器截图复核 `profile-layout-final.png`，文字不再覆盖技能圆形装饰。
- PlayMode：`TestResults/playmode-pdf-alignment-v2.xml` 37/37，通过四职业状态、骰子 1.15 精确文案、全套主要页面布局与交互回归。最终 WebGL 构建、正式 Pages 验证仍需在发布后更新结果。

## 验证记录

- 第一批选秀修复：EditMode 221/221，PlayMode 36/36 通过。其后新增战斗与详情修复，须重新验证；正式站尚为 `ce95986`。
- 当前第二批：`TestResults/editmode-qa-combat.xml` 223/223，`TestResults/playmode-qa-combat.xml` 37/37 通过；实测遍历当前全部 64 名角色，不使用旧文档人数限制覆盖范围。等待构建和发布验证。
- 通过独立浏览器从正式站查看首页、选秀、图鉴、角色详情、团队；未发生脚本异常。手机尺寸截图保存于 `Artifacts/qa-20260905/`，属于模拟尺寸，非 iPhone 真机。
- 后续仍须完整实测：首页、团队、成员、饰品、选秀、章节、战斗、结算，以及短屏和桌面。
- 真实 iPhone 加载恢复尚需用户设备复测，浏览器手机尺寸模拟不等于真机成功。
