# 战斗界面整体验收 — 2026-09-05

## 范围与基线

- 用户要求：修复整张战斗页的拥挤、重叠与角色不可辨认，不只修截图圈出的按钮。
- 从已上线 `63c1573` 对应源码 `721e6b4`（及其交接记录 `90b3182`）创建独立界面修复分支 `ui/battle-layout-20260905`。
- 暂未通过战斗平衡验收的 `db28c88`、`326c120` 保留在 `ui/chapter-map-starglass-redesign`，不混入本次 UI 发布。没有回退或删除它们。
- 大厅顶部 HUD、底部导航、章节地图、双面试池、四职业与 2.0 骰型上限保持已上线基线。
- 正式地址仍为 https://bojuejun.github.io/cho-siren-preview/ ，本地地址仅供代理内部验证。

## 修复

1. 战斗头部改为同高单排「自动 / 倍速 / 暂停」，与阶段、关卡名称和血条独立排布；退出移入真正阻挡底层点击的暂停菜单，可从菜单或右上角继续。
2. 原舞台背景已烘焙骰子台座、成员光圈，在下方操作区使用完整不透明深色底层遮挡，避免底图 UI 和代码 UI 双重叠加；舞台及骰子原美术不变。
3. 骰子保留状态移到骰面下方，不再把“成型·已保留”和数字编号堆在水晶上；透明点击热区保留，不恢复四边形底座。
4. 成员使用独立近景取景精灵，不修改共享角色原画；头像、名称、血条分区。前九位已有角色使用单独取景配置，其余角色使用通用上半身取景，后续新美术需逐一视觉确认。
5. 移除成员华丽套框，当前行动采用细描边，不改变邻卡大小；异常状态放在卡片底部，显示状态时隐藏同槽 HP 数字，保留血条。
6. 技能按钮简化为清晰的文字卡片，取消选中时放大；自动/敌方行动显示状态而非空面板，清除上一个手动目标提示。
7. 敌方血量、护盾、异常状态分行，避免同坐标覆盖。操作字号提升，动态长文本保留有限度缩放。

## 测试记录

- `TestResults/playmode-battle-layout-v1.xml` 未生成：新增测试直接访问 internal PanelKit 导致测试程序集编译失败，已改用公开成员数据读取资源。
- `playmode-battle-layout-v2.xml`：39/39 通过。
- `editmode-battle-layout-v1.xml`：225/225 通过。
- `playmode-battle-layout-v3.xml`：39/39 通过，含敌方状态分行最终检查。
- 已增加固定回归：单排按钮互斥且不碰血条/标题/阶段；退出只在暂停菜单出现；模态阻挡；下方旧背景 UI 被完整遮挡；骰子状态不压骰面/按钮；头像不与名字/HP/状态重叠；技能等待态非空；旧四角色原图不被替换且取景精灵复用。
- `playmode-battle-layout-v4.xml`：39/39 通过，增加真实中文字体 preferredHeight 检查，修复实机发现的成员标题和血量数字裁切。
- 首轮 WebGL 构建 `Builds/WebGL-BattleLayout-20260905` 成功；独立 Chrome 成功加载，无 pageerror。实际查看了首页、章节、手动战斗、自动与 2 倍速、暂停菜单、320×568 窄屏和恢复 390×844。截图在 `Artifacts/qa-20260905/layout-*.png`。
- 实机检查后继续修复文字行高及 Outline 透过半透明背景把整张成员/技能卡染亮的问题；这两项最终修复尚未重新构建 WebGL。**不要把现有 BattleLayout 构建当作最终源代码对应产物发布。**
- 用户随后要求为节省额度暂停。已停止进一步优化和发布，关闭内部浏览器与 HTTP 测试服务，保留源码及截图。正式网页仍为 `63c1573`，本轮 UI 尚未上线。
- `playmode-battle-layout-v5.xml` 为最后一轮颜色/行高回归：39/39 通过。

## 用户随后授权仅上线已完成改动

- 最终代码来源 `a1197ea`，新构建 `Builds/WebGL-BattleLayout-Final-20260905`；原首轮非最终包未发布。
- 最终包已实看 390×844、320×568 的单排控制、暂停/继续、头像、血量数字、成员标题、手动技能和自动 2 倍速胜利结算；另外实看首页 HUD/底部导航、章节地图与线上/线下面试池。截图 `Artifacts/qa-20260905/final-*.png`；Chrome pageerror 为空。没有真机 iPhone/Safari 复测，不将模拟尺寸等同真机兼容。
- 网页发布提交 `367faa9`，仅快进 `origin/main`；源码 `origin/master` 没有推送，继续保留之前的授权边界。站点页面壳仅四个资源引用变化，大厅视频逐字节不变。
- 正式 https://bojuejun.github.io/cho-siren-preview/?v=367faa9 已引用本次四个内容哈希；data `009935caeaa634399ce1647a6a64b89c`（74,289,207 字节）、wasm `a1d1c7b06dbd8e86916aed79126dc29b`（8,984,514 字节）线上 HEAD 均为 200 且大小一致；framework `89acae8be0b97dcf455ffbdd8db36b17`、loader `44f9a6b66fbffe98f3450e56703c6bb2`。
- 交付验证脚本通过全部 SHA-256 文件名/尺寸/引用/无多余资源检查，service-worker 语法检查通过。GitHub Actions 公共 API 遇到共享 IP 限流，未把 CI 状态当作已验证；通过正式站点实际资源与浏览器复核发布。
- 本次仅发布现有 UI 修改，完成后按用户节省额度的要求停止，不恢复持续优化。

## 后续整合注意

以后合并进行中的能量规则时，只适配骰子说明和按钮业务，保留本页坐标分区、暂停菜单和近景头像。不要用整文件回退或旧 WebGL 包覆盖该布局。视觉验收必须同时包含手动、自动、暂停、结算以及窄屏，不只检查静态矩形。
