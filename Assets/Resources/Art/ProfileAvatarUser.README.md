`ProfileAvatarUser.png` is the user-selected player avatar source imported on 2026-09-03.

The prior `ProfileAvatar` asset is intentionally retained so the selection can be reverted without data loss.
