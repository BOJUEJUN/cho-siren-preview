# CHO-SIREN 网页版本

## v0.3.1 — 2026-09-08 队长与骰子更新

- 源码实现：90ea9a1；WebGL 构建提交：0c94ab9。
- 统一头像选人、默认已拥有/全部筛选、搜索、队长任命与效果说明。
- 骰子伤害增益逐次相加，最多 +100%（×2）。
- 第一章限时120秒、三星100秒，末20秒警示；默认新号本机实测68.9秒通关。
- 480项Unity测试、12项网页测试通过。保留存档，不清档。

## v0.3.0 — 2026-09-07 回退基线

这是对已有0.3.0版本的明确冻结点，并非重建旧包。

- 网页提交：422a642；源码基线：4f381c2。
- 七部位装备、成员管理与战术战斗。

## 回滚约定

标签 `v0.3.1` 与 `v0.3.0` 指向冻结的网页发布快照，不移动已有标签。
`build-versions.json` 保留当前及上一版完整的四个哈希文件；这只是回退资源保留，不代表自动失败降级。

需要回滚时，从指定标签检出到独立目录，校验资源与 `npm run check`，再将该完整网页快照作为新的提交发布到 main。不要强推或重置主分支历史，也不要只更换index而遗漏build-versions.json。

回滚游戏代码和资源不等于回滚玩家存档。玩家数据保存在各自浏览器；涉及存档格式变动时，须先单独导出/备份并验证兼容性。本次更新不清理存档。
