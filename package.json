{
  "name": "cho-siren-preview",
  "version": "0.3.0",
  "private": true,
  "description": "CHO-SIREN Unity WebGL portrait game preview",
  "scripts": {
    "check": "node scripts/verify-webgl.mjs && node --check service-worker.js && node --test scripts/test-loader.mjs",
    "test": "node --test scripts/test-loader.mjs",
    "verify": "npm run check"
  },
  "engines": {
    "node": ">=20"
  }
}
